spring:
  datasource:
    url: ${datasource.url}
    username: EMEDADM_QA
    password: '3m3dAdm44ll01$#Med'
  data:
    mongodb:
      uri: mongodb://${mongodb.username}:${mongodb.password}@${mongodb.host}/qa?authSource=admin&authMechanism=SCRAM-SHA-1
  #Kafka properties
  kafka:
    bootstrap-servers: ${kafka.hosts}