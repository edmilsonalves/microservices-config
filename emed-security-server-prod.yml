spring:
  datasource:
    url: ${datasource.url}    
    password: 'EMdadm@!'
  kafka:
    bootstrap-servers: ${kafka.hosts}
  redis:
    host: ${redis.host}
    port: ${redis.port}
    #password: ${redis.password}
    repositories:
      enable: true