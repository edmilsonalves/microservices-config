spring:
  datasource:
    url: ${datasource.url}
    password: '3m3dAdm44ll01$#Med8'
  redis:
    host: ${redis.host}
    port: ${redis.port}
    #password: ${redis.password}
    repositories:
      enable: true
